# 인수인계 — 위베이프 "예약 및 AS 관리" 웹앱

> **다음 담당자에게**: 이 문서를 통째로 복사해서 클로드(Claude) 대화창에 붙여넣으세요.
> 그러면 클로드가 이 프로젝트의 상태를 전부 파악하고 바로 이어서 작업할 수 있습니다.
> 문서 맨 아래 "첫 지시문"까지 같이 붙여넣으면 됩니다.

---

## 0. 한 줄 요약

전자담배 매장 체인(위베이프)의 **미취급·품절 상품 예약 접수**와 **A/S 접수·처리**를 관리하는 사내 웹앱입니다.
10개 매장 직원 18명이 매일 사용합니다. 이미 실서비스 중이며 실제 데이터 1,084건이 들어 있습니다.

- **서비스 주소**: https://wevape-reservation.vercel.app
- **로그인**: 직원 이름 선택 + PIN 4자리 (전 직원 공통 `8936`)

---

## 1. 접속 정보 — 어디에 무엇이 있나

| 구분 | 위치 | 비고 |
|---|---|---|
| 소스 코드 | GitHub `ceohj0101/wevape-reservation` (브랜치 `main`) | 앱 전체가 `index.html` 파일 하나 |
| 배포 | Vercel · 팀 `wevapeforever` · 프로젝트 **`wevape-reservation-new`** | GitHub `main`에 push하면 자동 배포 |
| 데이터베이스 | Supabase · 조직 `wevape` · 프로젝트 `wevape-pos` | POS와 같은 프로젝트를 쓰되 테이블은 `resv_` 접두사로 분리 |
| Supabase URL | `https://jpxiyfwljxnraosleylg.supabase.co` | |
| Supabase anon key | `sb_publishable_D7Bsq7P3t2ug6uG0oFW8hA_zsbviCMR` | 공개용 키. `index.html`에 그대로 들어있음 (정상) |
| 원본 A/S 시트 | 구글 드라이브 "＊ 전 지점 A/S 현황" | 12개 지점 탭. 매장에서 직접 작성하는 원본 |

### ⚠ Vercel 프로젝트가 두 개입니다 — 헷갈리지 마세요
- **`wevape-reservation-new`** ← 실제 도메인이 붙은 **진짜 서비스**. 여기만 보면 됩니다.
- `wevape-reservation` ← 예전 백업. 도메인이 없어서 아무리 배포해도 실서비스에 반영 안 됨.

### GitHub 토큰
이전 담당자가 쓰던 개인 토큰(PAT)은 넘기지 않습니다.
새 담당자 본인 계정에서 **fine-grained token**을 새로 발급하세요.
- 대상 저장소: `ceohj0101/wevape-reservation`
- 권한: **Contents → Read and write** (이것만 있으면 됩니다)

---

## 2. 기술 구조 (아주 단순합니다)

```
index.html  ← 앱 전체. HTML + CSS + JS가 이 파일 하나에 다 들어있음. 빌드 과정 없음.
   │
   ├─ Supabase JS SDK (CDN)  → PostgreSQL 직접 조회/수정
   ├─ Chart.js (CDN)         → 통계 그래프
   └─ sw.js + manifest       → 바탕화면 설치(PWA)용
```

- 서버 코드 없음. 정적 파일만 Vercel이 서빙합니다.
- 프레임워크 없음(React 등 안 씁니다). 순수 JS입니다.
- **파일 하나(2,600여 줄)라 수정할 때 해당 함수만 찾아 고치면 됩니다.**

### 저장소 주요 파일
| 파일 | 용도 |
|---|---|
| `index.html` | 앱 전체 |
| `schema.sql` | DB 테이블·뷰·함수·권한 정의 (여러 번 실행해도 안전) |
| `staff_admin_functions.sql` | 직원 관리용 함수 + `no_answer` 상태 |
| `as_checked_column.sql` | 접수 확인 체크용 컬럼 |
| `as_reimport_2026-08-10.sql` | A/S 실데이터 1,082건 재이관 **(이미 실행 완료, 다시 실행 금지)** |
| `as_fix_dates_2026-08-10.sql` | 접수일자 오타 5건 정정 **(실행 완료)** |
| `as_fix_method_2026-08-10.sql` | 접수방식 미표기 272건 정리 **(미실행)** |
| `test/run-test.js` | 오프라인 자동 테스트 (113개 검증) |
| `test/mock-libs.js` | 가짜 Supabase/Chart.js — 테스트용 |
| `mockup-*.html`, `proto-*.html` | 지금까지 만든 디자인 시안들 (참고용, 서비스와 무관) |

---

## 3. 데이터베이스

### 테이블
| 테이블 | 내용 |
|---|---|
| `resv_stores` | 매장 10곳 |
| `resv_staff` | 직원 18명 (PIN 포함) |
| `resv_reason_options` | 접수 사유 목록 |
| `resv_product_requests` | 상품 예약 접수 |
| `resv_as_requests` | A/S 접수 (실데이터 1,084건) |

### 매장 (10곳)
구월로데오 · 연수 · 인천공항 · 논현 · 구월길병원 · 계산 · 부천상동 · 부천중동 · 을지로 · 검단
(폐업한 건대점·다동점은 제외했습니다)

### 직원 (18명, PIN 전부 `8936`)
- `manager`(실장): 실장님
- `admin`(관리자): 김형진
- `staff`(직원): 신재현, 홍다운, 장현진, 독고명록, 정희경, 김다정, 이종혁, 장대운, 조효정, 차영근, 고아현, 원주현, 정유진, 안태민, 김상훈, 최민영

### A/S 처리 흐름 (제품 구분 + 접수 방식에 따라 다름)
- **본사 제품 · 현장교체**: 접수 완료 → ERP 기입 완료 *(2단계)*
- **본사 제품 · 단순접수**: 접수 완료 → ERP 기입 완료 → 입고·연락 대기 → 인계 완료 *(4단계)*
  - 연락이 안 되면 **연락 두절**(`no_answer`)로 빠짐
- **타사 제품**: 지점 접수 → 로데오점 접수확인 → 제조사 발송·검사중 → 재입고 완료 → 지점 발송완료 → 고객 수령완료 *(6단계)*

> 현장교체 = 접수할 때 새 상품으로 바로 바꿔드린 건 (고객에게 다시 연락할 일 없음)
> 단순접수 = 물건을 맡아둔 건 (입고되면 고객에게 연락해서 인계해야 끝남)

### 상품 예약 흐름
문의접수 → 발주가능 → 입고완료 → 연락완료 → 구매완료
(옆갈래: 발주불가, 구매포기)

---

## 4. 화면 구성 (현재)

사이드바 메뉴 순서: **상품 예약 접수 → 예약 통계 → AS 접수 → AS 통계 → 설정**
로그인하면 상품 예약 접수 화면부터 열립니다.

### 공통
- 상단 **"보는 범위"** 드롭다운 — 전체 매장 / 매장 하나 선택. 예약·AS 화면이 같이 따라갑니다.
- 그 아래 **월 바** — 이번 달이 기본. 지난 달들, 전체 기간, 기간 직접 설정(시작일~종료일) 선택 가능.
  - 옆에 빨간 **"⚠ 지난달 이전 미완료 N건"** 버튼 — 밀린 건만 모아보기
  - 아래에 **매장별 접수 수량**이 선택 기간 기준으로 자동 집계
- **단계 스트립** — 그 화면의 처리 단계별 건수 타일. 누르면 그 단계만 아래 목록에 표시.

### AS 접수 화면
- 목록은 두 줄 압축형: 윗줄에 제품명+상태, 아랫줄에 접수일·매장·고객
- 각 줄 왼쪽에 **확인 체크박스** (실물 확인 표시) — 확인된 줄은 초록 배경
- 필터: 전체 상태 / 처리중 / 완료, 확인 전체 / 미확인만 / 확인 완료만, 고객명·등록자 검색
- 상세: 진행 상황 버튼(단계별), 이전 단계로, 정보 수정, 접수 삭제, 사진 추가

### 통계
- 예약 통계 / AS 통계 각각 매장·기간(월별 또는 직접 설정) 필터
- AS 통계: **제품별 A/S 발생 순위**(색상·사유 통합, 클릭하면 불량 사유별 수치), 중복 증상 TOP, 매장별 비교

---

## 5. 개발·배포 방법

### 코드 수정 → 배포
```bash
# 1) 저장소 받기
git clone https://github.com/ceohj0101/wevape-reservation.git
cd wevape-reservation

# 2) index.html 수정

# 3) 테스트 (필수)
node test/run-test.js      # 113개 검증이 전부 ok 여야 함

# 4) 배포 — push 하면 1분 내 자동 반영
git add -A && git commit -m "변경 내용" && git push origin main
```

> **Vercel에 직접 파일을 올릴 필요 없습니다.** GitHub push만 하면 자동 배포됩니다.

### 테스트에 대해
`node test/run-test.js` 는 진짜 DB 대신 메모리상의 가짜 데이터로
로그인 → 접수 등록 → 단계 진행 → 통계 → 설정까지 자동으로 클릭해보고 확인합니다.
**코드를 고쳤으면 반드시 돌려보고 push 하세요.** 실서비스라 사고가 나면 매장이 바로 멈춥니다.

### DB 변경
Supabase 대시보드 → SQL Editor → **새 쿼리 탭**에 붙여넣고 Run.
> ⚠ 1회성 데이터 SQL은 **한 번만** 실행하세요. 예전에 여러 번 붙여넣어서 336건이 3배로 중복 입력된 사고가 있었습니다.

---

## 6. 반드시 알아야 할 함정 (사고 났던 것들)

1. **A/S 날짜는 `received_date`를 쓸 것. `created_at` 아님.**
   과거 스프레드시트에서 옮겨온 건들은 DB 입력 시각과 실제 접수일이 다릅니다.
   정렬·표시·통계 기간 필터 전부 `received_date` 기준이어야 합니다.

2. **원본 구글시트 A열의 "월"은 접수일이 아니라 본사 발송월입니다.**
   빈 접수일을 이 값으로 채우면 날짜가 통째로 틀어집니다. 실제로 한 번 그렇게 해서 전면 재작업했습니다.
   접수일은 반드시 B열 "접수일자"만 쓰세요. 날짜 형식이 5종 섞여 있습니다
   (`2026. 3. 24` / `26-03-19` / `4/7` / `03월 15일` / `26.0809` 같은 오타).

3. **`resv_staff` 테이블은 직접 조회·수정이 막혀 있습니다.**
   PIN이 REST로 새지 않게 select 정책을 막아뒀는데, 그 때문에 update도 조용히 무시됩니다
   (오류 없이 성공처럼 보이는데 값이 안 바뀝니다).
   직원 관련 작업은 반드시 함수(RPC)를 쓰세요:
   `resv_staff_list`, `resv_staff_set_role`, `resv_staff_set_active`, `resv_staff_set_pin`, `resv_staff_add`

4. **Supabase는 한 번에 최대 1,000행만 돌려줍니다.**
   전체 건수를 셀 때 `.range(0,999)` 로 페이징하지 않으면 1,000에서 잘립니다.
   이것 때문에 "데이터가 누락됐다"고 잘못 판단한 적이 있습니다.

5. **구글시트를 텍스트로 변환해서 읽으면 뒷부분이 잘립니다.**
   반드시 xlsx로 내려받아 파싱하세요. 이것 때문에 1,082건 중 435건만 읽고 완료로 착각했습니다.

6. **Vercel 프로젝트 두 개 혼동 주의** (위 1번 항목 참고).

7. **작업 환경(클로드 샌드박스)에서 막히는 것들**
   - `api.vercel.com` 접근 불가 → Vercel CLI 못 씀. 배포는 git push로.
   - `*.supabase.co` 접근 불가 → DB 확인은 브라우저에 열린 앱에서 JS로 조회.

---

## 7. 지금 상태와 남은 작업

### 데이터 현황 (2026-08-10 기준)
- A/S 접수 총 **1,084건** (재이관 1,082 + 앱에서 등록 2)
- 지점별: 구월로데오 288 · 구월길병원 173 · 논현 164 · 부천상동 127 · 연수 112 · 계산 64 · 검단 57 · 인천공항 49 · 부천중동 43 · 을지로 5
- 접수일 없음 41건 (전부 구월길병원 — 원본에 날짜 대신 "초기불량", "시연용"이라고 적혀 있음)
- 발송월 기준 추정 85건 (메모에 표시해둠)
- 상품 예약은 아직 1~2건 수준

### 🔴 바로 해야 할 일 (SQL 미실행)
1. **`as_checked_column.sql` 실행** — 안 하면 목록의 확인 체크박스가 동작하지 않습니다(오류 안내가 뜹니다).
2. **`as_fix_method_2026-08-10.sql` 실행** — 원본에 접수방식 표기가 없던 272건을 '미확인'으로 되돌립니다.
   지금은 전부 '현장교체'로 들어가 있는데, 그중 실제 단순접수 건이 섞여 있으면
   "입고 후 고객 인계" 절차를 안 타서 고객이 기다리는 물건을 놓칠 수 있습니다.

### 확정 안 된 것 (사용자에게 물어봐야 함)
- **메뉴 카테고리 순서** — A/S 담당자가 순서 조정을 요청했는데 원하는 최종 순서가 불명확합니다.
- **"매장 통계"를 별도 메뉴로 뺄지** — 지금은 AS 통계 안에 매장별 비교가 들어있고, 접수 화면에도 매장별 집계가 있습니다.

### 남은 일반 작업
- 직원 17명 소속 매장 지정 (설정 → 직원 목록 → "권한·매장 변경")
- 접수방식 '미확인' 건 분류 (최근 2개월치 130건만 대상으로 하기로 함)
- 동일 내용 2줄로 들어간 20건 — 원본 시트에 실제로 2줄이라 진짜 2건인지 확인 필요
- 카카오 알림톡/문자 자동 발송 연동 (입고 후 고객 안내용, 발송 서비스 계정 미확보)
- **Supabase 무료 quota 초과 경고** — 대시보드에 "2026년 8월 30일부터 제한될 수 있음" 배너가 떠 있습니다. 유료 전환 또는 사용량 정리 필요.

---

## 8. 작업할 때 지켜온 원칙

- **한국어로**, 전문 용어는 괄호 안에 쉬운 말을 덧붙입니다.
- 코드 주석도 한국어로, "왜 이렇게 했는지"를 남깁니다.
- 기능을 바꾸면 `test/run-test.js`에 검증을 추가하고 전부 통과시킨 뒤 push 합니다.
- 데이터를 건드리는 작업은 **먼저 현황을 세어서 보여주고**, 애매하면 물어본 뒤 진행합니다.
- 실서비스라 추측으로 데이터를 채우지 않습니다. 모르는 값은 비워두고 화면에서 "미확인"으로 분류하게 합니다.

---

## 9. 다음 담당자를 위한 첫 지시문 (이 아래를 그대로 함께 붙여넣으세요)

```
위 인수인계 문서를 읽고 이 프로젝트를 이어받아 작업할 거야.

먼저 아래 순서로 파악부터 해줘:
1. https://github.com/ceohj0101/wevape-reservation 저장소를 clone 해서 index.html 구조를 파악할 것
   (필요하면 내가 GitHub 토큰을 새로 만들어서 줄게)
2. node test/run-test.js 를 돌려서 현재 113개 검증이 전부 통과하는지 확인할 것
3. 실서비스(https://wevape-reservation.vercel.app)를 열어서 현재 화면 상태를 확인할 것

파악이 끝나면 "남은 작업" 목록을 우선순위대로 정리해서 보여주고,
어디부터 할지 나한테 물어봐줘.

작업 규칙:
- 한국어로 설명하고, 전문 용어는 괄호 안에 쉬운 말로 풀어줘
- 코드 고치면 반드시 테스트 돌리고 통과한 뒤에 push 해
- 데이터 건드리는 작업은 먼저 현황을 숫자로 보여주고, 애매하면 나한테 물어봐
- 실서비스라 사고 나면 매장이 멈춰. 추측으로 진행하지 마
```
